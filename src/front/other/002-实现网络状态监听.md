# 002-实现网络状态监听

监听网络是否在线离线
```js
let inOnline = navigator.onLine;

// 开始监听网络状态的变化
window.addEventListener('online', () => {
    inOnline = true
});
window.addEventListener('offline', () => {
    inOnline = false
});
```